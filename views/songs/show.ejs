<%- include('../partials/header') %>

<section>
    <div>
        Song Title: <%= song.title %>
    </div>
    <div>
        Artist: <%= song.artist %>
    </div>
    <div>
        <%= song.caption %>
    </div>
&nbsp;
</section>
<section>
<h3>Comments</h3>
    <% if (user) { %>
        <form method="POST" action="/songs/<% song._id %>/comments">
            <textarea name="content">Let 'Em Know How You Feel</textarea>
            <input type="submit" value="Comment">
        </form>
    <% } else { %>
        <p><a href="/google/auth">Sign In</a> to Comment</p>
    <% } %>
</section>
<!-- MUST ADD STUFF TO VIEW COMMENTS STILL -->
<!-- ASK ABOUT POST /songs//comments (skipping ':id') -->